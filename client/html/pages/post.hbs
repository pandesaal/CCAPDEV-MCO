<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{posts.title}} - Archer's Freedom Wall</title>
    <link rel="stylesheet" href="../../css/pages/post.css">

    <!--importing icons-->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
</head>
<body data-page="post">
    <div id="navbar">{{> nav}}</div>
    <div id="register-wrapper">{{> register}}</div>
    {{> modals}}
    
    <main>
        <div id="wrapper">
            <div id="post-wrapper">
                <!-- This part will show a single post using the format from post.html -->
                {{> post posts fullContent=true}}
                <div class="comment-input-box">
                    <form id="createCommentForm" method="post">
                        <input name="comment-content" id="comment-input" placeholder="Add a comment..." />
                        <button><span class="material-symbols-outlined" type="submit">send</span></button>
                    </form> 
                </div>
            </div>
            <div id="commentsSection">
                <h3>Comments</h3>
                <div id="commentsList">
                {{#if posts.comments.length}}
                    {{#each posts.comments}}
                        {{> comment this}}
                    {{/each}}
                {{else}}
                    <p>No comments yet.</p>
                {{/if}}
                </div>
            </div>
        </div>
    </main>

    <!-- importing javascript -->
    <script type="module" src="../../js/pages/post.js"></script>
</body>
</html>